spring:
  profiles:
    include:
      - real
      - oauth
  jpa:
    show_sql: true
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    generate-unique-name: false
    username: sa
  session:
    store-type: jdbc
server:
  servlet:
    encoding:
      force-response: true
